{
  "name": "moshcode.sh",
  "version": "0.1.0",
  "private": true,
  "description": "moshcode.sh — the home page for moshcode, a metal wrapper CLI for agentic coding",
  "type": "module",
  "license": "MIT",
  "engines": { "node": ">=24" },
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node --watch src/server.mjs",
    "test": "node --test test/*.test.mjs",
    "sync:commands": "node scripts/sync-commands.mjs",
    "check": "node scripts/sync-commands.mjs --check"
  },
  "dependencies": {
    "express": "5.1.0"
  }
}
